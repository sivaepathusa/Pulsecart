# Submission checklist

Copy this into the repo as `SUBMISSION_CHECKLIST.md` and tick it before freeze.

- [x] `README.md` answers the five business questions with numbers
- [x] `README.md` says exactly how to rerun
- [x] `queries.sql` contains the six required statements
- [x] `analysis.ipynb` or `main.py` runs top to bottom
- [x] `requirements.txt` is present
- [x] `ai_use_log.md` is present (write `None` if unused)
- [x] At least four meaningful Git commits
- [x] No `last_active_date` used as a raw churn feature
- [x] Train/test split does not shuffle a time series
- [x] Repo URL sent to the training lead before the deadline
